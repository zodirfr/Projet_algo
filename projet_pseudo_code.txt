//PSEUDO-CODE

le package est main

importer "fmt" et "math/rand"

fonction main() {
	r ← générateur_aléatoire(seed = temps_actuel)

	Créer un tableau tableau de taille 10
	Pour i de 0 à 9 (pas de 1) :
    	tableau[i] ← nombre aléatoire entre 0 et 4

	Créer un tableau occu de taille 5
	Pour chaque élément v dans tableau :
    	occu[v] ← occu[v] + 1

	
	Afficher(tableau)
	Afficher(occu)
}
