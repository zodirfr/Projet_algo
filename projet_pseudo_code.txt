//PSEUDO-CODE

le package est main

importer "fmt" et "math/rand"

fonction main() {
	r ← générateur_aléatoire(seed = temps_actuel)

	var tableau [10]int
	pour i qui est égale à 0; i doit être inferieur à 10; par pas de 1 
		tableau[i] = r.Intn(5)
	
	
	Créer un tableau occu de taille 5
	Pour chaque élément v dans tableau :
    	occu[v] ← occu[v] + 1

	
	Afficher(tableau)
	Afficher(occu)
}
